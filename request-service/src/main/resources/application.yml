server:
  port: 8084
spring:
  application:
    name: request-service
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/blood_bank_db?currentSchema=request_schema}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: request_schema
